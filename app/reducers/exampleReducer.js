
//import * as types from '../actions/actionTypes';

const initialState = {

};

let exampleReducer = (state=initialState, action) => {

    switch (action.type) {


        default:
            return state;
    }
};

export default exampleReducer;